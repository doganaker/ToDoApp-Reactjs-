import React from 'react'
import { Modal, Button } from 'antd';

function AddButton() {
    return (
        <>
            <Button style={{ backgroundColor: 'lightgreen', borderRadius: '10px', color: 'white' }}><strong>Add</strong></Button>
        </>
    )
}

export default AddButton
